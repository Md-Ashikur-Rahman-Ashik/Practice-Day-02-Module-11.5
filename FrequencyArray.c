#include <stdio.h>

int main()
{
    int array_size;

    scanf("%d", array_size);
    int arr[array_size];

    for (int i = 0; i < array_size; i++)
    {
        /* code */
        scanf("%d", &arr[i]);
    }

    int countFrequency = 0;

    for (int i = 0; i <= 9; i++)
    {
        /* code */
        for (int j = 0; j < array_size; j++)
        {
            /* code */
            if (arr[j])
            {
                /* code */
            }
            
        }
        
    }
    

    return 0;
}